import * as React from 'react'
import {
    container
} from './showCase.module.css'

const ShowCase = ({  }) => {
    return (
        <div className={container}>

        </div>
    )
}

export default ShowCase